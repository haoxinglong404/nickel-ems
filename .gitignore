# 操作系统生成文件
.DS_Store
Thumbs.db
desktop.ini

# 编辑器配置
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code 本地状态
.claude/
CLAUDE.local.md
*.local.md

# 临时文件
*.tmp
*.bak
*.backup
*.orig

# 构建产物（本项目不应该有）
node_modules/
dist/
build/

# 密钥和敏感信息
.env
.env.local
*.key
*.pem
secrets.json

# 原始数据和备份（不该 commit 到仓库）
*.xlsx
*.xls
*.csv
/raw_data/
/backups/

# 种子数据 JSON（这些应该保留在本地，不 commit 到仓库）
# 如果 seed 逻辑已经内联到 index.html 里就不需要
# equipments.json
# lube_points.json
# spare_parts.json
# imported_workorders.json

# 测试生成的中间文件
/tmp/
*.log

# 一次性运维 F12 脚本（用户手动跑，不进仓库）
*_F12脚本.js
清空*.js

# 润滑/审核本地工作文件（生成器 + 待改清单 + 审核 HTML，不进仓库）
_gen_lube_audit.py
_pending_lube_fixes.json
*审核.html

# 五定表模板填充脚本（本地跑，数据从预览会话导出，不进仓库）
填五定表_*.py
