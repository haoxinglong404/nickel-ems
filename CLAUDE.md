# Claude Code 项目备忘录 · Nickel Plant EMS

> **⚠️ AI 助理：每次会话开始时请完整阅读此文件，不要跳过任何"重要约定"。**
> 历史改动的完整纪要在 `HISTORY.md`（不自动加载，查某次改动细节时再读）。

---

## 🎯 项目本质

**这是什么项目**：印尼某湿法冶镍车间的设备管理 Web 应用（单页面 + 云数据库）。

**谁在用**：1 位设备管理员（非技术人员）+ 4-6 位工人。移动端为主，宽屏次之。

**关键属性**：
- 🏭 **生产环境** — 代码直接影响实际车间运作，不是玩具项目
- 👥 **多人协作** — 数据通过 Firebase 实时同步，一人改全团队秒同步
- 📱 **移动优先** — 按钮 ≥44px、输入框 ≥52px、字号不小于 13px
- 🔒 **数据不能丢** — 删除都要"撤销/硬删除"双轨制

---

## 🏗️ 技术架构（**一句话原则：不引入框架**）

```
单文件 index.html  →  Firebase Firestore  →  GitHub Pages
（HTML+CSS+JS）      （实时数据库）          （静态托管）
```

**Firebase 项目**：`nickel-ems` · 区域 `asia-southeast1`（新加坡）
- 认证：匿名认证（`signInAnonymously`，2026-05-03 上线）+ 工号登录 + 客户端密码校验
- 数据库：Firestore · **白名单安全规则已上线**（`firestore.rules`；改集合结构要同步 rules 并在 Console 发布，**漏 `meta` 会导致启动中断**）
- 存储：暂未使用

**Firestore 集合**（每个文档的 id 字段要和 Firestore doc id 一致）：
- `users` · 用户账号（id = 工号字符串）· 含 `wechatMobile` 字段（当前无实际用途，见"关键常量"企业微信段）
- `equipments` · 设备台账（**845** 台 = 酸浸 422 + 中和 305 + **产品 66** + **制酸 52** · id = `eq_seed_XXXX` / `eq_new_XXX` / `eq_v6_XXX` / `eq_zh_NNN` / `eq_cp_NNN` / `eq_zs_NNN`；**产品车间 08-11 主动把台账砍到只剩 A/B 类 66 台**〔车间自删 239 台 C 类 + 我方删 22 台槽罐〕，此前是 327 台〔`eq_cp_038`/`041` 两台早先丢失未补〕；**恢复源 = 微信文件夹 `产品车间设备台账（新）.xlsx`**）· `abcClass` 关键性等级（''/A/B/C，352 台已赋级 = A30/B69/C253，70 静设备未分类）· 342 台已改直观名（晨曦/海湾/N系列 前缀，命名规则见 memory project-equipment-rename-scheme）
- `maintenancelogs` · **检修记录**（id = `ml_时间戳_随机`）—— 2026-07-01 替代已删的工单模块；选二级库备件自动扣库存（联动 secondstock/secondstockhistory）
- `todos` · **当个事儿办**（缓办事项清单 · id = `td_时间戳_随机`）—— 2026-07-27 上线；可关联设备可不关联，**07-30 起支持多台**（新字段 `equipments[]`=[{id,tag,name}]，旧单设备字段 `equipmentId/Tag/Name` 保存时镜像首台、读取靠 `tdEqs()` 兼容老记录）；`status` pending待办/ready已做好准备/done已办 + `deleted` 软删（显示叫「不办了」，可恢复无彻底删除）；仅 admin 可写（UI 层拦截）
- `lubepoints` · 润滑点（**1820** 个 = 酸浸 893 + 中和 486 + 产品 441 · id = 酸浸 `lp_XXXX` + 历次拆点/补点系列 `lp_v6_*` ~ `lp_v22_*`（各系列来历见 HISTORY.md）/ 中和 `lp_zh_NNN` / 产品 `lp_cp_NNN`）
- `lubehistory` · 润滑执行记录（**1857** 笔 = 酸浸 653 + 中和 717 + 产品 487 · id = `lh_<lubeAt时间戳>_<4位随机>`）
- `secondstock` · **二级库（现场库存台账）**（id = `ss_imp_NNN` 种子 / `ss_时间戳_随机` 新增）
- `secondstockhistory` · 二级库出入库历史（id = `ssh_时间戳_随机`）—— 带 `mlId` 字段的 = 检修记录的**账本投影**（每次保存检修记录整体重写：每备件恒一笔出库=当前用量，删记录笔就消失；**禁手动撤销/删除**）
- `inspecttemplates` · **点检模板**（**64 个** · id = `tpl_<key>`）—— 56 个 v2 结构化模板（`v:2` + `groups[{g,pts:[{p,ms:[{n,t:'num'/'chk',u,max,min,std,mth}]}]}]`，含离心泵 5 种密封变体/搅拌器 4 变体 + **14 个中和 `zh_` 模板**（2026-08-08，浓密机·电驱 8.0mm/s、凉水塔 6.5/80）+ **11 个产品 `cp_` 模板**（2026-08-10 按产品车间点检表转录 9 个，浓密机=液压油泵主电机5.0/驱动减速机8.0，新增 压滤机/皮带机/起重机 模板，电机振动块无异响项 `_vibT4`；**08-11 补 2 个名字带「（临时）」的** `cp_compressor` 空压机·产品吹干 26 测点〔照 `zh_compressor_oilfree` 改，M355-W10-2S 是两级压缩故只留一/二级温度〕、`cp_screw_conveyor` 螺旋输送机·产品 24 测点〔照 `cp_belt` 改：去托辊/滚筒/跑偏/刮料板，换螺旋叶片+首尾轴承座+中间吊轴承+进出料口〕——**产品车间点检表里没这两类，正式表到位后替换**） + **7 个制酸 `zs_` 模板**（2026-08-12 按《设备点巡检表（制酸车间）》13 张表转录，**限值按他们的表：电机<85℃/泵<80℃/减速机<70℃**，与前三车间的 70℃ 不同；`cycleDays:1`；离心泵分带电流/不带电流两个变体〔炉前地下槽泵那张表没有电流列〕；振动标注保留原表写法——基本型用 `ⅠA/ⅠB/ⅡA/ⅡB/Ⅰa/Ⅰb`、给水泵型用「水平/垂直/轴向」，**不强行统一否则工人对不上纸质表**；`zs_so2_blower` 测点带 DCS 仪表位号〔606TI11/606VI04…〕且有 3 个区间限值〔润滑油压 2~3.5Bar、密封气 534~605mbar、油温 20~50℃〕））+ 8 个 v1 勾选模板（items 数组；静设备无"设备停机未运行"项）；每个模板带 `rev`（=`INSPECT_TPL_REV`）内容修订号，**改模板内容必须 +1 并配新迁移 marker**（只加新 key 不用动 rev）
- `inspecthistory` · 点检执行记录（id = `ih_时间戳_随机`）—— v2 设备只在**有异常时**写（带 `src:'v2'` 供历史 tab 去重），v1 设备每次写
- `inspectmonthly` · **月度数值点检表**（id = `im_<eqId>_<YYYYMM>`）—— 一台设备一月一文档，`days["日"]={at,by,byId,stopped,v:{测点key:数值或0/1},abn:[超限key],notes:{},note}`；**只订阅当月+上月**（where month in）→ 读取量恒定；同一天重复提交 = 覆盖当天（订正通道）
- `meta` · 一次性迁移幂等标记（id = `*_v[N]_YYYYMMDD`）
- ~~`workorders` / `spareparts` / `spareparthistory` / `tools` / `toolhistory`~~ —— **已废弃**（工器具/旧备件 2026-06-21、工单 2026-07-01 整体删除，云端已清空；rules 里 workorders 白名单暂留待删）

---

## 🚨 绝对不要做的事（血泪教训）

### ❌ 禁止引入任何构建工具

不要用 webpack、vite、npm install 任何东西。项目就是一个 `index.html`，直接浏览器打开就能用。

### ❌ 禁止分拆文件

不要把 CSS/JS 拆出去。用户直接在 GitHub 网页上传 `index.html` 就是部署 —— 这是**用户的核心工作流**，不能破坏。

### ❌ 禁止修改字段命名

下面这些字段名绝不能改（Firestore 已有历史数据）：

| 集合 | 字段 | 说明 |
|---|---|---|
| lubepoints | `operationType` | `'oil'` / `'grease'` —— **但显示分类按 standardOil 是否含"脂"判断**，见 `getLubeKind()` |
| lubepoints | `maintenanceFree` | `true` = 免维护（`getLubeStatus()` 返回 `'exempt'`，不进逾期统计）|
| secondstockhistory | `type` | `in`（入库）/ `out`（领用）|
| secondstockhistory | `reverted` / `isCounter` | 撤销双轨标记，别用其他字段名 |
| secondstockhistory | `mlId` | 检修账本投影关联；带此字段的记录禁手动撤销/删除 |
| maintenancelogs | `category` | `fault` / `planned` / `preventive` |
| maintenancelogs | `deleted` | 软删（作废）标记；检修记录无彻底删除 |
| equipments | `abcClass` | `''` / `A` / `B` / `C` 关键性等级 |
| secondstock | `location` / `fieldLocation` | 领出库位（只读保留）/ 现场库位（列表/筛选用后者）|
| 全部业务集合 | `plant` | 车间归属：缺省=hpal 高压酸浸 / `'zh'` 中和 / `'cp'` 产品。**`equipments` / `lubepoints` 两个集合 08-11 已全量补写显式 plant**（为了服务端 `where(plant==)` 过滤省读取，marker `plant_backfill_v24_20260811`）——这两个集合每条都有该字段，可放心用 where 查；**其余集合仍保持"老数据勿补写"**，只能靠 `docPlant()` 的 `|| 'hpal'` 兜底做客户端过滤 |
| equipments | `noInspect` / `inspectTplKey` | 不进点检标记 / 指定点检模板 key（中和分批上点检用）|

（已废弃模块 workorders/spareparts/tools 等的字段约定已随模块删除失效，旧文见 git 历史。）

### ❌ 禁止用 localStorage 存业务数据

只能用来记：
- 当前会话 token（`SESSION_KEY`）
- 用户偏好（如上次联系方式自动填充）

所有业务数据必须在 Firestore。

### ❌ 禁止删除"首次启动种子数据"逻辑

`seedIfEmpty()`、`seedLubeIfEmpty()`、`seedInspectionTemplatesIfEmpty()`、`seedSecondStockIfEmpty()` 这些函数**保留不删**。它们只在云端对应集合为空时才写入，幂等安全。（`seedPreviewInspectDemo()` 仅 `?preview=1` 且云端月表空时注入内存演示数据，不写云端。）

---

## ✅ 必须遵守的命名和约定

### 位号命名规则

格式：`{区域}-{专业}-{类型}-{序号}{分支}`

示例：
- `201-PE-AG-001A` · 201 区域 · 工艺设备 · 搅拌器 · 001 · A 分支
- `316-1-MT-PP-003` · 316-1 区域 · 金属设备 · 泵 · 003

**绝对不要**生成不符合这个格式的位号。如果需要给设备加位号，先参考 Firestore `equipments` 集合里现有的格式。

### 文件改动必须满足

1. **每次修改 `index.html` 都要**：
   - **本机无 node**：语法校验用 Python 括号/引号/注释配平器 + preview 加载后查 `typeof window.fn` 全为 `function` + 控制台 0 报错
   - 修改后文件大小不能异常变化（正常约 **1.0 MB**，seed 常量占大头）
   - 验证所有 `onclick` 调用的函数都通过 `window.fn = fn` 暴露
   - 内联 `oninput="someVar.x = this.value"` 也要求 `someVar` 在 window 上（模块作用域 `let` 不行 —— 用 `Object.defineProperty(window,...)` 桥接，见 mlBuffer/ssTxnBuffer 的套路）

2. **改 HTML 结构时必须**：
   - 如果加新 module（section），要同步更新 `switchModule()` 的所有相关逻辑
   - 如果加新顶栏，要在 `switchModule()` 里控制显隐

3. **改样式时必须**：
   - 用现有的 CSS 变量（`var(--ink)`、`var(--g-deep)` 等）
   - 不要加内联 `style="color: green"`，没意义且难维护

---

## 🎨 设计系统（不要偏离）

**色板**（这些变量已定义在 `:root`）：
```css
--paper:   #FAFAF7  /* 米白背景 */
--ink:     #1A2A1A  /* 墨绿·正文 */
--g-deep:  #3A5424  /* 镍钴绿·深 */
--g-forest:#27500A  /* 镍钴绿·森林 */
--g-mist:  #F0F5E8  /* 镍钴绿·雾 */
--g-primary:#97C459 /* 镍钴绿·亮 */
--muted:   #6B6A66  /* 次级文字 */
--subtle:  #9B9A96  /* 辅助文字 */
--line:    #E4E0D8  /* 分隔线 */
```

**字体栈**：
- `--font-serif` = Cormorant Garamond（大标题）
- `--font-sans` = Inter（正文、按钮）
- `--font-mono` = JetBrains Mono（标签、数字、等宽）

**圆角**：主卡片 `14px`，次级 `10px`，胶囊按钮 `999px`。

**间距**：8px 网格（4/8/12/16/20/24/32）。

---

## 🔄 典型工作流：加新功能的正确姿势

用户说："给检修记录加个导出 Excel 的按钮"。

**正确的流程**：

1. **先读 `ARCHITECTURE.md`** 了解现有模块结构
2. **用 Grep 工具**搜代码里是否已有导出相关代码（避免重复）
3. **规划改动**：哪里加按钮？用什么 JS 库？（能不加就不加 → 先看 xlsx-js-style 是否已引入）权限：谁能导出？
4. **提议方案**给用户确认（中文 + Markdown 格式，有结构、有取舍）
5. **动手改代码**，每一步精确操作、assert 成功
6. **验证**：Python 配平校验 + preview 实际加载 + 确认函数被 `window.` 暴露 + 检查 onclick 语法
7. **交付**：告诉用户"做了什么、怎么测试、已知限制"

---

## 📝 用户偏好（郝兴龙）

- **不要用英文解释**。必须中文为主，英文术语要配翻译。
- **不要让用户做技术决策**。我问"您想用方案 A 还是 B"之前，先给推荐。
- **不要给太多选项**。2-4 个最好，超过 5 个用户会困惑。
- **修改 UI 前先给视觉说明**（用 SVG/HTML 预览，而不是纯文字描述）。
- **严谨**：每次 str_replace 要 assert 成功，不能静默失败。
- **动手之前**告诉用户"我准备做 X、Y、Z"，让她确认。
- **每次代码改动 push 后**，必须更新本 CLAUDE.md 的"下次会话应该知道的上下文"段落：压缩版（1-3 行）追加到"最近做的改动"，**完整纪要追加到 `HISTORY.md` 顶部**；移除已完成的待办；有数据迁移则更新 marker 表。保证新会话能无缝接上项目。

---

## 🎭 角色矩阵（权限设计必须参考这个 · 2026-08-07 多车间权限重构）

**10 角色**：管理员（admin，仅郝行龙）/ 经理（manager）/ 设备工程师（equip-engineer）/ 设备技术员（equip-tech）/ 生产员工/班长/主任 / 检修员工/班长/主任。**账号带 `plant` 车间归属**（hpal/zh/all，缺省=hpal），角色显示自动带车间前缀（「中和·设备工程师」，all 不带）。

| 权限 | 管理员(郝) | 经理(李宁翔/鹏总·all) | 设备工程师=技术员 | 其他 6 角色 |
|---|---|---|---|---|
| 检修/二级库/当个事儿办/设备台账 | ✓ | ✓ **所有车间** | ✓ 本车间 | 只读 |
| 账号管理 | ✓ 全部 | ✓ 全部（动不了 admin 账号、授不了 admin）| ✓ 本车间(非admin) | ✗ |
| 点检提交/润滑登记 | ✓ | ✓ 所有车间 | ✓ 本车间 | ✓ **限本车间** |
| 硬删历史 / 二级库改时间 | **仅郝行龙本人**（`HARD_DELETE_EMPNO`/`SS_TIME_EDITOR_EMPNO` 按工号硬判）| ✗ | ✗ | ✗ |

**规矩**：出了自家车间一律只读（顶栏提示「👀 参观中·只读」），经理/管理员除外；登录自动落到自己车间（all 留在当前）。工程师和技术员权限完全相同，只是职级名不同。

**账号现状（2026-08-12 核实，共 43 个）**：酸浸 23（缺省无 plant 字段 22 + 显式 hpal 1）· 产品 6 · 中和 5 · **制酸 0（唯一空缺）** · `plant:'all'` 跨车间 9（李宁翔/鹏总/解总/陈总 经理、宁晖/陈晓帆 工程师、何总 检修主任、钟红刚 检修班长、**`Test` manager ← 早期测试账号，建议删或降权**）。**别再照 v0.20 那条纪要里的"9 个账号"办事，那是 08-07 的快照。**

权限 helper：`userPlant` / `inHomePlant` / `isEquipStaff` / `canManage`（业务管理总闸）/ `canEditEquipment` / `canManageUsers` / `canTouchUser` / `canPickPlant` / `canSubmitField`（点检润滑）/ `canHardDelete` + 老的 `isAdmin` / `isManager` / `canEditMl` / `canEditSSTime`。（工单时代的 `canApproveWO` 等仍残留，已无调用场景。）**新写入口必须套 canManage/canSubmitField,别再裸用 isAdmin。**

---

## 🔑 关键常量和入口

- **管理员初始账号**：郝行龙（2026-07-27 由「总管理员」改实名）· 工号 `6725102247` · 密码 `15086370152hxl`
- **李宁翔**：工号 `6724013003` · **2026-08-07 起为 经理·全部车间**（此前是副管理员）· 登录弹专属欢迎卡（`VIP_WELCOMES` 文案仍称"翔总"）；同日鹏总（6724031101，经理）也标 `plant:'all'`
- **VIP 配置**：搜 `VIP_WELCOMES` 常量，添加新 VIP 在这里加
- **Firebase 配置**：搜 `firebaseConfig` 常量
- **SESSION_KEY**：`ems_session_v06`（localStorage 里的会话键 · 2026-08-06 订正，此前本文误记为 `nickel_ems_session`）
- **多车间常量**（2026-08-07）：`PLANT_KEY='ems_plant_v01'`（当前车间）· `USER_CACHE_KEY='ems_user_v01'`（上次登录账号快照 {empno,name,role} 无密码，秒进应用用）· `PLANTS`={hpal:高压酸浸车间, zh:中和车间, cp:产品车间(2026-08-10 加), **zs:制酸车间(2026-08-12 加)**} · helper `docPlant/inPlant/stampPlant/switchPlant`。**加新车间只需往 `PLANTS` 加一个 key** —— 全库已无硬编码车间列表（2026-08-12 全文搜 `'cp'` 零命中确认过），UI/筛选/权限全是 PLANTS 驱动
- **二级库改时间白名单**：`SS_TIME_EDITOR_EMPNO = '6725102247'`（郝行龙本人，非全体 admin）· helper `canEditSSTime`
- **企业微信群通知（当前失效 · 用户 2026-07-22 决定先留着）**：`notifyWeChat()`（index.html 约 3707 行）原 5 个调用点全在工单模块，随工单删除后**无任何调用方**——现在不会发任何群通知；"我的"页面手机号设置入口（`wechatMobile` / `confirmSetMobile`）仍在但填了无效果。Cloudflare Worker `https://ems-notify.haoxinglong404.workers.dev` · token `ems-2026-nickel-secret-x9k2` 仍部署着。若恢复 = 把 notifyWeChat 挂到检修/点检事件；若放弃 = 删函数 + 设置入口 + Worker。

---

## 🚀 下次会话应该知道的上下文

当前版本：**v0.26**（v0.25 制酸车间接入 → **08-15 重点设备看板按领导手绘改版：设备方框网格 + 就地展开零件卡 + 预热器给料泵进重点设备 + 零件消耗表替代原统计图表**）

> ⚠️ **产品车间台账已被砍到 66 台（08-11）**：车间自行删除 239 台 C 类设备 + 我方按其要求删 22 台槽罐，台账 327→**66 台（=A11+B55，与点检范围完全重合）**。后果：**441 个润滑点里 271 个成了孤儿**（所属设备已不存在，润滑模块仍能正常显示/登记/导出，但点不进设备详情、关联不了检修、不进 ABC 统计）。**产品车间以后要登记检修/领备件/查铭牌，得先把设备加回台账** —— 恢复源是微信文件夹里的 `产品车间设备台账（新）.xlsx`。删掉的点检记录备份在桌面 `产品车间台账收窄备份_2026-08-11.json`。

> **多车间（2026-08-07 起）**：单数据库，全部业务集合用可选 `plant` 字段区分车间——`'hpal'` 高压酸浸（**老数据没有该字段 = hpal，勿补写**）/ `'zh'` 中和。9 个订阅回调 `.filter(inPlant)` 过滤缓存，15 处写入 `stampPlant()` 盖章;`users`/`inspecttemplates` 两车间共用不过滤。当前车间 `currentPlant` 记在 `localStorage['ems_plant_v01']`，切换入口=顶栏徽章/「我的」首行，切换=写 key + reload。设备可选 `noInspect:true` = 不进点检（中和分批上点检用）、`inspectTplKey` = 指定点检模板。**新增写云端的代码必须过 stampPlant**，否则该数据两个车间都看不见的一边会错。

**底部导航**：重点设备看板 / 设备 / 点检 / 检修 / 二级库 / 润滑 / 我的（**横滑 flex**；**⚠️ 08-17 起只有酸浸是 7 项，中和/产品/制酸只剩「设备·点检·我的」3 项** —— 开关 `FULL_MODULE_PLANTS`，见下方最近改动第 1 条，改这几个模块前先想清楚只有酸浸看得见；「重点设备看板」6 字长，窄屏该项单独 `flex:0 0 86px`，宽屏侧栏规则在 @media 内位置更后仍覆盖为 auto；界面文字 2026-07-21 起「巡检」全站改叫「点检」，函数名/集合名未动）
> **重点设备看板**（`board` module，`data-nav="board"`，2026-08-03 提升为顶级模块，**2026-08-15 按领导手绘整体改版**）：**全员只读**，全部按当前系列统计（1/2/3 系列胶囊，默认1系列）。结构从上到下：①**设备方框网格**（`mlbEqGridHtml`）——高压釜给料泵 2 台 / 高压釜搅拌器 7 台 / **预热器给料泵 10 台**（新进重点设备，warman/五二五 两厂家分开两片、方框名去厂家后缀；每系列=中预一/二级+高预一/二/三级×A/B），方框显示状态（⚠零件超寿命=橙框 / 累计N次 / 暂无记录）；点方框**就地展开零件卡**（`mlbEqExpandHtml`，状态 `mlBoardOpenTag`）——每零件一行：上次更换/在用天数/平均寿命/次数 + **「在用：备件名」**（`mlbPartMainSpare` 挑主件一条，附件不显示），点零件行→单台档案页该零件时间线（`mlbOpenEqlog(tag,pkey)`，返回时展开保持）；②**零件消耗表**（`mlbConsumeCardHtml`）——每零件一行：本月消耗/本季度消耗（自然月/季，**数量只认名字带零件关键词的主备件**，垫片附件不计）/平均寿命；③**厂家/规格寿命对比卡 ×2**（单向阀 + 预热器泵机封，`mlbBrandStats`/`mlbBrandCardHtml` 通用函数，同样只认关键词命中的主备件，垫片归「未登记厂家/规格」）。**原概览卡/超寿命预警卡/环形图/各台条形图/近6月趋势已删**（函数一并删除）。实时刷新：`subscribeMaintenance` + `subscribeSecondStock` 回调各接一根（后者管备件现名）；**board 的 MODULE_SUBS 含 `secondstock`**（mlSpName 要靠它显示二级库现名）。从看板进单台档案（`mlEqlogFrom='board'`）导航高亮停在看板，`mlbBackToBoard()` 返回。登录后默认落地页仍是「设备」。
> **检修模块**（`maintenance` module，`data-nav="maintenance"`，沿用原工单骨架 list/detail/create 三页 + `mlBuffer` 桥接）：**仅 admin 可登记/编辑/作废/恢复**，其他人只读；**无彻底删除**（只可作废=软删可恢复，作废时库存自动回补）。字段 `equipmentId/Tag/Name` · `category`(fault/planned/preventive) · `repairDate` · `faultParts[]`(标签多选) · `faultCause` · `measures` · `spareParts[]`(可从二级库挑/手填，**二级库的自动扣库存·手填的不扣**) · `repairerName` · `note` · 软删 `deleted`。常量 `ML_CATEGORY_MAP` / `COMMON_FAULT_PARTS` / `ABC_CLASS_MAP`。设备详情"相关记录"tab 读 `mlCache`。列表有类别筛选 + A/B/C 等级筛选（可叠加）+ 重点设备条（**给料泵/搅拌器/预热器给料泵三组**，`ML_KEY_EQ_GROUPS` 包含式匹配；预热器泵 2026-08-15 加入，match `^[123]系列-(中预|高预)[一二三]级泵`，唯一零件=机械密封）→ 三级页 `maintenance-keyeq` / `maintenance-eqlog`（单台档案：记录 tab + 零件更换时间线按故障部位 tags 精确归类 + 寿命对比图）。段头「📊备件消耗」汇总导出（跟随当前筛选）。列表标题旁 **📋记录/🗒️当个事儿办 两个独立视图按钮**（`mlView` 两态 + `setMlView(v)` 直选，`applyMlView` 控制显隐/标题/FAB，同页切换非新 module；看板是顶级模块 `board`，现行设计见上方「重点设备看板」段）。重点零件定义 `MLB_PART_DEFS`（**搅拌器和预热器泵的 pkey 都叫 `mechseal`——按零件统计的循环必须过滤 `d.gkey===s.gkey`，否则双计数**）；备件归属 `mlSpareOwner`（组内 parts 顺序先命中先得；隔热组件 kw 含构成件 导杆/导套/衬套/活塞/分离盘/O形圈 等，单向阀 kw 含「阀盖」）。⚠️ `renderMlCard(ml, from)` 有第二参，不能直接 `.map(renderMlCard)`。
> **当个事儿办**（检修页第三视图 `mlView='todo'` · 集合 `todos` · 2026-07-27）：缓办事项清单，"暂时不干、准备后面干的活"——可关联设备也可不关联；状态 `pending`待办→`ready`已做好准备→`done`已办，**「待办」tab = pending+ready**（备好了还没干依然算待办）；「不办了」=`deleted` 软删可恢复（无彻底删除，仅 admin 可见该 tab）；仅 admin 可 记一件/编辑/流转/不办了/恢复，其他人只读。**已办/不办了确认走应用内底部弹层 `showSheet('tdDone'/'tdDelete')`——别用原生 prompt/confirm（内置浏览器面板会拦掉，点了没反应）**。详情/编辑子页 `todo-detail`/`todo-edit`（返回回 todo 视图）；设备选择复用 `pickEquipment` sheet 按 `eqPickTarget` 路由（**待办侧多选**：点选/再点取消+「完成（已选 N 台）」；检修侧单选即关，入口 `mlOpenEqPicker` 显式切 'ml'）；**关联设备可多台**（`equipments[]`，表单胶囊 × 移除；卡片/详情多台位号同前缀省略 `tdAbbrevStrs` + 各带 ABC 徽章 + 「共 N 台」；卡片位号下有设备名行，多台同名只显一次）；`tdBuffer` defineProperty 桥接；标题旁按钮带未办结计数。
> **设备 A/B/C 分级**：`abcClass` 字段，设备卡/检修记录/详情显徽章（`.abc-badge`），设备编辑页有下拉。**本地 SEED 故意不同步 abcClass**（主 SEED id 按位置生成，注入易错；生产读云端不受影响）。桌面 `设备ABC分级清单_草稿_2026-07-01.xlsx` 留档。

**模块结构**：
- 二级库（`parts` module，标题 Second Stock）：现场库存台账，**带结存的库存账**
  - 字段：`name`/`materialCode`/`sourceNo`/`unit`/`fieldLocation`(现场库位)/`location`(领出库位·只读)/`requestQty`/`inQty`/`quantity`(结存)/`usagePart`/`applicant`
  - module：`parts`(列表)/`parts-detail`(详情)/`parts-txn`(领用/入库)/`parts-edit`(新增/编辑)/`parts-history`(出入库历史·全员可看)
  - **仅 admin** 可领用/入库/编辑/删除/批量导入；其他人只读
  - 历史 type：`in`/`out`，带撤销(reverted/isCounter)+硬删双轨；带 `mlId` 的检修投影笔禁撤销/删除，显「🔧 检修」标签可跳检修详情
  - 辅助函数：`escapeAttr` / `renderInfoRow` / `ssDataSource`(预览模式云端空时回退 SEED)；`ssTxnBuffer`/`ssEditBuffer` 用 defineProperty 桥接 window
- 润滑模块顶部 3 tab：🛢️ 脂润滑 / ⛽ 油润滑 / 📝 待补充油号（**按 standardOil 是否含"脂"分**）
  - **列表按设备分组**：一台设备(同位号+同油种)=一张卡，多点位用 `renderLubeGroupCard`(含「一键登记全部」`openBulkLube`/`confirmBulkLube`：列需润滑点位，免维护自动跳过，writeBatch 各记一笔+顺延 nextLubeAt)、单点位用 `renderLubeCard`。分组 `groupLubePointsByDevice()`，徽章/筛选取最严重点位状态 `lubeGroupStatus()`。
  - **概览按设备统计**：4 个概览卡(逾期/即将/未初始/全部)=设备台数(每台归最严重状态，互斥)；列表计数「X 台设备 · Y 个点位」。
  - **免维护电机**：`maintenanceFree:true` → 'exempt'，排除统计（一键登记也自动跳过）；**现共 97 个免维护点**（酸浸 96：潜水密封电机/刮泥机行走升降/PAM·PAC 加药搅拌电机 + 07-27 新增闪蒸密封槽搅拌器 3 台与浓硫酸输送泵 6 台的 18 个电机轴承点 + 08-07 絮凝剂二次稀释泵 103A 电机前/后 2 点·现场核实无加油孔；中和 1：08-10 导入时 `109-205-PE-PP-006A` 倒料泵电机·原表标「无加油孔」）。液下渣浆泵电机不算免维护。**标记写法**（对齐现行 63/76 主流约定）：`maintenanceFree:true` + `status:'免维护'` + `standardAmount/periodDays/periodHours`、`lastLubeAt/nextLubeAt` 全置 null，**`standardOil` 保留**。
  - **电机=前/后轴承两点（仅酸浸口径）**：155 个需润滑电机各拆前/后轴承（`lp_v16_NNN`，前后各按原加注量）；免维护和无加注量电机不拆。**中和/产品不拆**（08-10 导入时用户拍板照各车间自己的五定表，一台电机=一个点：中和 146 个「电机」、产品 111 个「电机轴承及其他部位」）——同一个"电机"点在三个车间粒度不同，做跨车间统计时留意。
  - **打的下数**：黄油枪 1 下≈0.95g，`greasePumps(g)` 向下取整；`usesGreaseGun()` 排除联轴器/齿圈/开式齿。
  - 页头「📋 历史」→ `lube-history` 整页（全部/脂/油 tab + 自然日筛选 + 按天分组 + 导出）；「↓导出五定表」`exportLubeWudingXlsx`（内部自助格式；**正式交表走 HISTORY.md 里的填模板流程**）。

**已运行的一次性迁移**（marker 都在 `meta/` 集合。已废弃模块的 V4/V5/V9/V10 marker 文档仍在云端，但迁移函数已随模块删除，不会重跑）：
| Marker | 做的事 |
|---|---|
| `eq_seed_fix_v2_20260421` | 修 312A/B 位号冲突 + 删 201B 重复 |
| `eq_fix_v3_20260422` | 62 个位号规范化（TBD→正式 + 316-1 空格修复）|
| `eq_lube_fix_v6_20260423` | 修 10 个孤立润滑点 + 拆 002/003 设备 + 加 102B + 313B 润滑点 |
| `add_motor_lp_v7_20260423` | 202-PE-PP-005 高压水泵·电机润滑点 |
| `area_lube_v8_20260423` | 218/923/546 区 107 个新润滑点 |
| `inspect_init_v11_20260429` | 上传 17 个巡检模板（按 equipment.type 关联）|
| `inspect_tpl_v2_20260704` | 巡检模板升级 v2 数值点检结构 |
| `plant_backfill_v24_20260811` | 给 `equipments`(422) / `lubepoints`(891) 里缺 `plant` 的酸浸老数据补 `plant:'hpal'`，使订阅能用服务端 `where(plant==)` 过滤（读取量优化）。语义等价于 `docPlant()` 缺省兜底，归属零变化。**失败不写 marker，下次启动自动重跑（只处理仍缺字段的，幂等）** |

**最近做的改动**（压缩版·按时间倒序·**每条的完整细节见 `HISTORY.md`**）：
1. **非酸浸车间只保留「设备台账 + 点检」**（08-17，纯代码 3 处·无云端变更）：中和/产品/制酸的 **重点设备看板 / 检修（含当个事儿办）/ 二级库 / 润滑** 四块整体隐藏，底部导航只剩「设备·点检·我的」，设备详情页的「相关记录」「润滑点」两个 tab 一并隐藏。开关 = `FULL_MODULE_PLANTS = ['hpal']` + `LIMITED_MODULES`(17 个模块名 Set) + `plantHasFullModules()/moduleBlocked()/applyPlantModuleUI()`；接线三处：`enterApp` 调 `applyPlantModuleUI()`、`switchModule` **第一行**加守卫（在 `ensureSubs` 之前，被挡则 toast + 回退 equipment）、导航项 `display:none`。**一刀切按 `currentPlant`，不看角色**（用户明确要求）——管理员/经理切过去也看不到。**要给某车间放开只改 `FULL_MODULE_PLANTS` 一行**，其余全自动。顺带这三个车间不再订阅 maintenancelogs/lubepoints/secondstock 等集合，读取量再降。验证：抽真实源码 + 真实导航 DOM 拼桩页面 32 项断言全过 + esprima + 页面 0 报错。
1. **8月第3周润滑计划补录 36 笔 + 101A 补建 2 个电机脂点**（08-17，云端数据 + 1 处 SEED）：两张纸质表 36 行全执行——高预泵电机 12（8-10）+ 高压釜2~7隔室搅拌电机 12（8-11）+ 给料泵 106A/B 电机 4（8-12，实际 95/80g 超规定）+ 浓密机 1#~8# 减速机轴承 8（8-14，各 1000g）；操作人**给料泵 4 行何甫林、其余 32 行王宗立**（两人均非注册用户，`operatorId:''`）。照片里糊掉的「#减速机油箱」靠云端反查确定 = `202-PE-TH-101` 的 1#~8# 减速机轴承。**顺带查出台账缺口：`203-PE-AG-101A`（1系列1隔室搅拌）21 台隔室搅拌里唯一只有 2 个点**，缺电机前/后轴承——铁证是 id 缺号（每台连号"电机前+机封"，101A 只有 `lp_0336` 机封、`lp_0337` 空），建库时漏建，`lp_v16_*` 拆后轴承时因无前轴承点跟着漏；**这也是纸质表排不出 1 隔室搅拌的原因**。已补建 `lp_v22_001/002`（照 101B~G：51g/3#锂基脂/125天/3000h/未初始，本周不补记录）。`lubehistory` 1820→**1857**、`lubepoints` 1818→**1820**。口径同第1/2周（lubeAt=执行日 10:00、note「第3周润滑计划（8.10-8.14）补录」）。**顺带观察：`users` 现 48 个账号**（下方待办里记的 43 是 08-12 快照，未逐一核名单）。
1. **v0.26 重点设备看板按领导手绘改版**（08-15，纯代码·无云端变更）：①设备方框网格主视图（给料泵2/搅拌器7/**预热器泵10 台新进重点设备**，warman/五二五分开两片），点方框就地展开零件卡（上次更换/在用/平均/次数+「在用：备件名」），点零件行进单台档案定位该零件时间线；②原概览卡/超寿命预警卡/环形图/条形图/趋势图全删，换**零件消耗表**（本月/本季度消耗+平均寿命）；③厂家寿命对比卡泛化 `mlbBrandStats` 通用函数并加预热器泵机封一张，**全部改按当前系列统计**；④统一「主备件」口径：名字带零件关键词才计消耗/进对比，垫片附件不计；⑤修双计数 bug（搅拌器/预热器泵 pkey 同名 `mechseal`，统计循环补 `d.gkey===s.gkey` 过滤）；⑥board 订阅加 `secondstock`（显示二级库现名如「（达索）」）+ 二级库回调接看板刷新线。
1. **登录两处收尾修复**（08-12，纯代码 2 处·无云端变更）：都是前三刀带出来的副作用/缺口。①**认证慢时密码框不展开 → 点登录误报「密码错误」**：工号框改防抖+认证就绪才查之后，认证慢时密码框不会出现，用户没地方输密码，`pw` 为空撞上 `user.password` 判定 → 报「密码错误」，会让人以为自己记错了。改为**先展开密码框 + 提示「该账号需要密码，请输入密码后再试」+ focus**。②**长轮询降级标记原来没有解除路径**（弱网抖一次就永久走长轮询，流量高延迟增）：标记值从 `'1'` 改存**置位时间戳**，**3 天后自动过期回到默认连接**，真有问题会当场重新置位；老格式 `'1'` 向后兼容（读到时补上时间戳从当时起算）。验证：登录桩测 30→**36 项**、IndexedDB 桩测 12→**20 项**，全过。
1. **iOS Safari 登录第三刀：连接通道自动降级（强制长轮询）**（08-12，纯代码 4 处·无云端变更·**✅ 用户线上验证通过，登录已恢复正常**）：第二刀部署前的那次尝试最终报出 **`timeout`（不是 `auth-timeout`）**——说明**认证通了、卡在查账号**，两次各 10 秒都没回话。除 IndexedDB 外的第二个元凶：**Firestore 默认走 WebChannel 流式连接，Safari + 某些运营商网络会被中间设备掐断且不报错、只是永不返回**。改法：①新增 sticky 标记 `ems_net_degrade_v01`，**登录连续超时即置位，下次启动自动 `experimentalForceLongPolling:true`**（默认不开，长轮询更费流量延迟略高）；②失败文案改成「连不上服务器，已切换兼容连接模式，请刷新页面后重试（timeout）」+ **登录页亮出「刷新页面重试」按钮**；③诊断行支持 sticky 消息（`loginDiagSticky`），降级形态常驻显示「兼容模式：长轮询连接 / 内存缓存」——**截图即可知当前跑在哪种模式**；④已在兼容模式还超时 = 真连不上，不重复标记、给普通网络文案。验证：登录桩测 26→**30 项断言**全过 + IndexedDB 桩测 12 项全过。
1. **iOS Safari 登录「一直连接中」第二刀：绕开 IndexedDB**（08-12，纯代码 5 处·无云端变更）：第一刀上线后按钮**确实变成「连接中…」但一直不动**，且密码框都没展开 → 说明**匿名认证根本没返回**（不是失败）。病因：**WebKit 上 `indexedDB.open()` 有概率永远不回调**（既不 success 也不 error，常见于无痕/存储被 ITP 清过/上次异常退出），而 **Firebase Auth 默认持久化就是 IndexedDB**、Firestore 的 `persistentLocalCache` 也等它 → 认证和查询一起挂死。改法：①**`getAuth` → `initializeAuth(app,{persistence:[browserLocalPersistence, inMemoryPersistence]})`**，用 localStorage（同步、不会挂）彻底绕开 IndexedDB —— **这是 WebKit 上公认解法**；②Firestore 加 `probeIndexedDb()`（4 秒不回调即判挂起）+ localStorage 标记 `ems_idb_bad_v01` 做 **sticky 降级**（下次启动自动切 `memoryLocalCache`，探测恢复则自动解除；**故意不用顶层 await 挡在前面**，那会推迟 `window.fn` 绑定使按钮报错，代价是第一次仍卡但刷新即好）；③**登录页加诊断行 `#login-diag`**（「云端认证中…」/「① 等待云端认证…」/「② 查询账号…」/「认证失败：<code>」）——**以后工人截图就能定位卡在哪一步**；④超时 15s/10s → 8s/8s（最坏 36s→17s）。验证：新增第二套桩测（真实源码抽取 + 动态 import 绕模块缓存）6 场景 12 断言全过 + 原 26 断言重跑全过。
1. **iOS Safari 登录失败/点不动 修复**（08-12，纯代码 4 处·无云端变更）：根因是**登录按钮和匿名认证在赛跑** —— 按钮在模块求值时就可点（`window.attemptLogin`），而 `init()` 在文件末尾才跑；认证没就绪时 Firestore 规则拒绝 → `getDoc` 抛错，兜底的 `usersCache` 又要等 `await authReady` 后的订阅才填 → 两条路同时死 = 「工号不存在或网络异常」（**这句跟工号无关**，工号真查不到是另一句）。**「点不动」是同一个病**：拿不到 auth token 时 Firestore 不报错而是**无限期挂着**（`getDoc` 没有超时）。**Safari 特别容易是因为 ITP 会清掉 7 天没访问站点的 IndexedDB（Firebase 登录态在里面），每次冷启动都要重新握手**；用户「手动输入好一些」正是佐证（手敲 30 秒够握手，密码本 2 秒不够）。改法：①认证拆成 `authUser`/`authPending`/`ensureAuth()` 可查可等**可重试**（失败置空 pending，下次点登录重发）；②新增 `withTimeout()`，登录先等认证（15s）再查账号（10s+重试一次）；③按钮 `disabled`+「连接中…」+防连点；④错误文案分「认证没到位/网络不通/工号不存在」三类**并带错误码**（截图即可判型）；⑤`localStorage.setItem(SESSION_KEY)` 补 try/catch（**原来没包，Safari 无痕抛异常会中断整个登录＝点不动**，是第二个真 bug）；⑥`onEmpnoInput` 加防抖 500ms + 认证就绪才查（**原来每敲一位发一次 getDoc，10 位工号最多 10 次读**）；⑦按钮补 `id`/`type="button"`，加 `pageshow` 恢复时重发认证。验证：从 index.html **正则抽取真实源码**拼桩模块（本容器无 jsdom），定时器 1/500 缩放，11 组场景 26 项断言全通过（含"永远挂起的 getDoc 现在 47ms 超时且按钮恢复可点"）。
1. **v0.25 制酸车间接入：52 台 A/B 类设备 + 7 个模板 + 点检频次规则改动**（08-12，代码 2 处 + 云端数据）：第四个车间。①**只导 A/B 类**（用户拍板 C 类不要）——原表 37 行/125 台里 A12+B40=52 台，位号是"一行多台"的压缩写法（`P-604101A/B~301A/B`=6台、`P-605101~301`=3台），按**子项号后第一位=系列号**展开并命名（1#/2#/3#），A/B 分支不拆（并联备用泵同名，同酸浸口径）；`P-609002` 撞号（含酸污水泵 vs 精炼浓硫酸输送泵）按用户指示前者改 `P-609002-TBD`；610 子项位号无横杠（`P61001A/B/C/D`）照原样保留。②**7 个 `zs_` 模板**见上方 inspecttemplates 段。③**改了点检频次规则**：`getInspectCycleDays` 原来"有 abcClass 就完全忽略模板 cycleDays"，制酸 B 类会被压成 2 天，但他们现场**每天点检 12 次**（两小时一轮，纸质表就是这么设计的）→ 改成 `Math.min(等级周期, 模板周期)`。**对前三车间零影响**（酸浸/中和模板全 7 天、产品 1/3/7，等级周期都 ≤ 模板值，min 结果逐条不变，已用 10 个断言验证）。④**制酸一天在系统里只记 1 次**（用户拍板，纸质表照旧 12 次），否则 `inspectmonthly` 的 `days["日"]` 一天一条存不下。⑤`PLANTS` 加 `zs`——全库无硬编码车间 key，加一行即可。⑥润滑五定表制酸还没给，润滑模块暂空。
1. **设备详情「润滑点」tab 复活 + 云端状态灯修正**（08-12）：①**那个 tab 从来没出现过**——`enhanceDeviceDetailTabs` IIFE 只赋给 `window.openDetail`/`window.switchDetailTab`，又被文件末尾 11189/11196 行的 `window.X = X` 覆盖回模块作用域版 → tab/panel 从未注入。**修法：不再玩动态注入**，tab 和 `#panel-lube` 直接写进 HTML（搜 `data-tab="lube"`），lube 分支并进真正生效的 `switchDetailTab` 包装（搜 `_origSwitchDetailTab`），整段死 IIFE 删掉。**读取量**：刻意不用 `ensureSubs('lubepoints')`（那要付 891/486/441），改 `loadEqLubePoints()` 按 `where('tag','==',eq.tag)` 定向查这一台（通常 1~5 条）；已进过润滑模块则 `subStarted.has('lubepoints')` 直接跳过、0 读取。新增 `eqLubeExtra`(Map)/`eqLubeLoaded`/`eqLubeLoading` + `deviceLubePoints(tag)` 合并缓存与定向结果；查询在途显示「加载中…」（否则会误报"暂无润滑点"）；**`openLubeDetail` 加了 `|| eqLubeExtra.get(id)` 兜底**——从该 tab 点进润滑点详情时 `lubePointsCache` 可能还是空的，不然会误报"润滑点不存在"。②**云端状态灯启动后一直停在黄色**（老问题）：`seedIfEmpty()` 末尾设绿，紧接着 `seedLubeIfEmpty()` 开头设成「检查润滑数据…」的黄，后面十几个迁移都在 marker 检查处提前 return 没人再拨回来 → 在所有种子/迁移跑完后补一句 `setCloudStatus('online',...)`。**语义**：绿=忙完一切正常 / 黄=正在连接或保存 / 灰=断网 / 红=出错。验证：jsdom 桩测新增 3 组断言（tab 与 panel 在 HTML、死 IIFE 已删、点击激活、不触发整集合订阅、冷启动走 `where tag==` 定向查且只查 1 次、已有全量订阅时跳过、启动结束圆点为 `rgb(156,192,98)`）全通过。
1. **v0.24 读取量优化线上验证通过 + 内联 favicon**（08-11 晚）：验证走的是**读 marker 文档的 `stats` 快照**而不是看控制台（迁移只跑一次、日志不会再出现）——补写数恰等于酸浸台数、zh/cp 一条没误改，通过；线上酸浸设备页 422 台吻合。顺手用一行 `data:image/svg+xml` 内联图标消掉了长期存在的 `favicon.ico 404`（不新增文件，配色取 `--g-deep`/`--g-primary`）。
1. **点检页等级筛选：台数为 0 的等级不再显示**（08-11 晚，纯代码 1 处）：产品车间台账砍到 66 台后 C 类清零，筛选行仍挂着「C 类 0」——`renderInspectFilterRow` 里 A/B/C 是硬编码遍历的（区域/类型那两行按实际数据聚合，本来就不会出 0）。[index.html:8923](index.html:8923) 加一句 `if (!n && inspectClassFilter !== k) return;`，**正被选中的等级即使 0 也保留**（否则按钮消失后取消不掉筛选，卡在空列表）。实测：产品只剩「全部66/A11/B55」、酸浸照常四个（C219）、选中C类→按钮出现→再点取消→隐藏，控制台 0 报错。
1. **产品车间台账砍到 66 台（车间自己的要求）**（08-11 晚，纯云端数据·无代码变更）：接用户"把设备台账也改成那 66 台、非 66 台的点检记录也删掉"的指令，排查时**发现云端 equipments 已从 1054 掉到 815 —— 产品车间少了 239 台**（plant 字段无错乱、酸浸 422/中和 305 完好，是记录真被删了），用户确认**是产品车间的人自己删的**。我方补删剩余 **22 台槽罐**（TK 静设备）→ 产品台账 **66 台**，并删 **85 条月表 + 6 条点检历史**（非 66 台的），删后孤儿 0。**删前已整体备份到桌面 `产品车间台账收窄备份_2026-08-11.json`**（22 台设备 + 85 条月表〔吕聪 70 / 钟红刚 15，全是 08-11 当天的真实点检〕+ 6 条历史的完整原始文档）。**润滑点按用户指示原样保留 → 441 个里 271 个成孤儿**（其余数据"有 Excel 可恢复"，故只备份点检记录）。⚠️ 我提过两条异议、用户确认后仍执行：①台账≠点检范围，`noInspect` 已能控制点检清单，删台账等于产品车间在系统里没有资产台账；②271 孤儿润滑点会断开设备详情/检修关联。**教训：这是本项目第一次出现"车间自己在生产库删数据"，以后动产品车间数据前先核对总数。**
1. **按车间订阅（读取量优化第二刀）：冷启动 ≈1100 → ≈435/318/340**（08-11，**含一次性云端迁移**·rules 无变更·**待用户线上验证**）：上一刀做完后最大固定开销变成 `equipments` 每次开 App 付 1054、`lubepoints` 每次进润滑模块付 1818 → 改服务端 `where('plant','==',currentPlant)` 过滤，降到 **equipments 422/305/327 + lubepoints 891/486/441**。拦路的是 **Firestore 查不了"字段不存在"**，而酸浸老数据按原约定没写 `plant` → 新增一次性迁移 `backfillPlantIfNeeded()`（marker `plant_backfill_v24_20260811`）给缺字段的补 `plant:'hpal'`（**语义等价：补的值就是 `docPlant()` 现在返回的值，归属零变化**；只补 plant 不动 updatedAt，免得 422 台设备看起来被人改过）。新增 `plantFilterReady` 标志 + `plantQuery(col)` helper：**marker 在才用服务端过滤，检查失败/补写失败一律退回整集合订阅**（显示结果一样只是多花读取），marker 最后写故中断可幂等重跑；`init()` 里 `await backfillPlantIfNeeded()` **必须在 `ensureSubs` 之前**，否则 hpal 用户会先用 `where(plant=='hpal')` 查到空列表。订阅回调里的 `.filter(inPlant)` **保留不动**做兜底。迁移本身一次性读 2872 写 1313。**`lubehistory`/`inspectmonthly` 没加 plant 过滤**——它们已有 `where lubeAt>=` / `where month in`，再加一个字段就成复合查询，要去 Console 建复合索引，不值。⚠️ **这与"老数据勿补写 plant"的原约定相反，是用户当场拍板的**，且只针对这两个集合。验证：jsdom 桩测 4 个场景（稳态酸浸/中和/产品 + 首次部署跑补写）全通过。
1. **Firebase 读取量优化 v0.24：冷启动 ≈9600 读 → ≈1100**（08-11，纯代码·无云端/rules/迁移变更·**待用户线上验证**）：①**4 处种子判空原来在整集合扫描**（users 13 + equipments 1054 + lubepoints 1818 + secondstock ~300 ≈3200 白读）→ 全改 `query(col, limit(1))`；②**按需订阅**——新增 `SUB_FNS`/`subStarted`/`ensureSubs(...names)` + `MODULE_SUBS`（20 个模块映射，挂在 `switchModule` 第一行），`init()` 里 11 行 subscribe 改成一行 `ensureSubs('users','equipments')` 常驻（≈1070 读），其余 9 个集合进模块才订、全程只订一次；设备详情「相关记录」tab 点开才订 maintenancelogs；③**lubehistory 只订近 90 天**（`where lubeAt>=`），缓存改 `lubeHistWindowDocs`+`lubeHistExtra` 两段按 id 合并（既有 6 个读取点一行没动），三条补全通道：`loadPointLubeHistory()` 按 lubePointId 定向查（**周期 330 天的点 90 天内可能一笔没有，必须补**）、`ensureFullLubeHistory()` 换整集合订阅（历史页选「全部」/导出五定表/导出明细，**五定表按点位列历次润滑必须全量**）；④离线缓存 `enableIndexedDbPersistence`（已弃用+多标签页会失败）→ `initializeFirestore` + `persistentLocalCache` + `persistentMultipleTabManager`。**没做的两项及原因**：`secondstockhistory` 不限期（`mlStockLedger`/`buildMlStockPlan` 靠全量按 mlId 找旧出库笔，限期会**算漏旧笔导致库存扣错**）、`maintenancelogs` 不限期（看板寿命统计/超寿命预警依赖完整历史，且本身才百来条）。**踩坑：`switchDetailTab` 被包了三层，8758 行 IIFE 那层只赋给 window 又被 11196 行覆盖回去 = 死代码**，第一版改到了死代码里，已挪到 7392 行真正生效的那层；同理 `window.openDetail` 也被 11189 行覆盖 → **设备详情页的「润滑点」tab 从来就没被创建过**（见待办）。验证：`node --check` + **jsdom+Firebase 桩运行时测试**（断言启动只订 2 个集合、各模块按需订阅与映射一致、不重复订阅、90 天窗口、选「全部」升全量）全通过；本容器 egress 屏蔽 gstatic 无法端到端连 Firebase。
1. **中和补拆 20 台范围名 + 产品点检收窄到 66 台 + Firebase 配额告急**（08-11，纯云端数据·无代码变更）：①**08-10 那轮扫描漏了两种写法**——前缀重复型 `CCD1-CCD6`（`-` 右边不是裸数字，正则要写 `[~\-]\s*(?:[A-Za-z]+)?(\d+)`）和逗号列举型 `CCD3,4,5,6`/`1#,2#`；补拆中和 20 台（6 台浸出渣CCD浓密机 + 6 台静混 + 4 台静混 1#/2# + 4 台一/二段除铁铝浓密机），刷 225 条快照。**坑：`lubepoints`/`lubehistory` 存设备名的字段是 `name` 不是 `equipmentName`**（`point` 才是点位名），第一批按 equipmentName 刷导致 215 条一条没动。②按用户给的《产品A、B类设备明细》核对：**66 台等级 100% 一致 0 冲突**（系统多的 32 台 B 是槽罐静设备，该表只列动设备，不冲突）；产品车间嫌范围宽，**点检 220→66 台**（关掉 165 台全 C 类，产品点检记录 0 条无损失）。③顺带查出**产品 2 台设备云端丢失**（`eq_cp_038`=109-207-PE-PP-007A 污水泵、`eq_cp_041`=109-207-PE-AG-008A 污水坑搅拌器，台账都有、无软删标记；08-10 丢过 `eq_cp_254` 是第二次）——**用户决定不补，产品定格 327 台**。④产品 26 台重名按明细表加编号（尾渣隔膜泵1#~6#、产品压滤机1#~4# 等），刷 151 条快照。⑤补 2 个「（临时）」模板 `cp_compressor`/`cp_screw_conveyor` → 产品在检 66 台（A11+B55）、8 个模板、无模板设备 0。⑥**Firebase 免费额度爆了**：24h 读 21 万 vs Spark 上限 5 万/天（UTC 零点重置=印尼早 8 点）——根因是 9 个集合全量订阅，**一次启动 ≈6500 次读 → 5 万只够开 8 次**；实测读写订阅都通（报错那次是 `getCountFromServer` 聚合查询的独立限流，别拿它推断全局停服），**读写配额独立，已打开的客户端照样能提交点检**。优化待做见下方待办。**本次已改用 `where('tag','in',[...])` 定向查询（按 10 个分块），26 台改名全流程仅约 380 次读，全量扫要 9000+；以后核对/批量改云端一律定向查询，不许全量扫库。**
1. **中和 + 产品车间润滑数据导入（927 点 + 1204 笔历史）**（08-10，纯云端数据·无代码变更·用户已验收）：两车间润滑模块此前**一条数据没有**（891 点/616 历史全是酸浸）→ 解析两份《设备润滑台账》五定表 xlsx（各 2 sheet 油/脂）导入 **中和 486 点(脂248/油238)+717 笔、产品 441 点(脂185/油256)+487 笔**，全库 lubepoints **891→1818**、lubehistory **616→1820**，位号 0 条对不上台账、历史 0 条挂空点位。**解析坑**：产品表每组记录 7 列（多「监督人」）、中和 6 列，必须按第 3 行子表头定位不能按固定宽度切；列名两表不一致（`规定用油量（g)` vs `规定用油量`、`换油周期` vs `正常换油周期`）需模糊匹配。**口径**：①**产品脂表单位是 kg 要 ×1000**（中和脂表头已写 g 直接用），油一律 L；②周期 `天=小时÷24 再取整到十位`（对齐酸浸 891 点现有映射：8000h→330天、2160h→90天）；③操作人（李贞/郭永亮/印尼工人/山西安装等）非注册用户，`operatorId:''` 按名字记；④lubeAt=实际日 10:00；⑤**3 种油品名不含"脂"会被 `getLubeKind` 分错 tab** → 补后缀归位（宝力达 EM 电机脂 / shell Omala 460（万向节脂）/ EMPL35LB（电机脂））。**电机点按各车间自己的表不拆前后轴承**（与酸浸 lp_v16 口径不同，用户拍板）。**位号处理**：产品 44 行写法差异走 alias 映射到云端位号（隔膜泵 `109-927-PE-PP-0101`→`PP0101` 等）；中和 36 个浓密机稀释搅拌（`TH-001A~D`）挂主机 `TH-001`、点名写「N#稀释搅拌·机架轴承」；`109-206-PE-PP-003` 判定为漏写 `003B`（原表连着 003A/003/003C）；545 那 4 台 PAC/PAM 加药计量泵挂的是撞号的 `FR-001-TBD` 加药装置**不是** `FR-001` 过滤器。53 条「出厂已加油」/只填动作无日期的记录不导入（点位留未初始）。**当日跟进：用户抽查发现"第1次润滑比初次还早" → 产品车间 57 笔跨年笔误 2025→2026 全部订正**（刘运涛 54 笔 + 印尼工人 2 笔 + 毕研祥 1 笔；中和 0 笔）；判据不能只用"倒序"（005C 第2次 2025-07-18 相对第1次递增不算倒序），要用**"产品车间最早的初次润滑=2025-12-04，早于此的物理上不可能"**才扫得全；连带 56 个点位 lastLubeAt/nextLubeAt 重算，删旧 57+写新 57+更新 56 放一个 writeBatch 原子提交（`lh_<lubeAt>_<rand>` 的 id 含时间戳故须重建，已确认 id 不被代码解析）。**教训：以后导入带时间戳的历史数据（润滑/点检/检修）必加两条扫描——①同一对象内时间递增 ②全部落在业务可能区间（不早于设备投用、不晚于今天）。**
1. **设备名"范围写法"全部拆成具体编号（产品 56 + 中和 68 = 124 台）**（08-10，纯云端数据·无代码变更）：用户在点检列表看到「4#~10#一段沉镍钴槽搅拌器」7 台同名 → 两轮改完，**全库已无范围名**（酸浸 07-09 那批早清理过）。规则=组内按「**位号尾部**数字+字母」排序（不能按整串 tag 排，`109-207-` 与 `109B-207B-` 前缀会乱序），第 i 台 = 范围起始号 + i，把名称里的范围串替换成 `{n}#`——**同一条规则通吃三种位置**：开头（`1#-4#一段除铁铝槽搅拌器`→`1#…`）、词尾（`工艺用空气储气罐1#~5#`→`工艺用空气储气罐1#`）、词中（`CCD1-6浓密机进料混合槽搅拌器`→`CCD1#…`）。**扫描正则注意 `#?`**：`(\d+)\s*#?\s*[~-]\s*(\d+)\s*#?` —— 少写第一个 `#?` 会漏掉 `1#-4#` 这种，少写末尾 `#?` 会漏掉 `CCD1-6` 这种（第一版就漏了 6 台）。**交叉验证**：槽体与搅拌器在台账里分组方式不同（槽拆 `1#~2#`/`3#~6#`、搅拌器写 `1#~6#`），两条独立路径算出的编号完全对应（`TK-003C`=3#槽 ↔ `AG-003C`=3#槽搅拌器），中和 5 组槽/搅拌器配对同样全对。**历史快照同步**：产品车间 0 条（刚上线全未点检）；中和刷了 **19 条 inspectmonthly + 1 条 inspecthistory** 的 `equipmentName`（08-07/08 那批除铁铝·预中和搅拌器点检记录）。
1. **产品车间 ABC 分级导入 + 补回丢失设备**（08-10，纯云端数据·无代码变更）：用户给动/静设备清单（带"设备分类"列）→ 321 台按清单写 abcClass，清单未含的 8 台（542/543 雨水回用水泵 6 + 1#/2#分配器 2）用户口头拍板补 C —— **329 台全部有等级：A11（5 浓密机 + 6 尾渣隔膜泵）/ B87 / C231，零空缺**。**点检频次自动切换**：A=1天/B=2天/C=7天（原来全走模板 3 天），222 台在检设备变为 A11/B44/C167。**顺带发现 `eq_cp_254`（109-207-PE-TK-003C 3#~6#二段沉镍钴槽）在云端丢失**（导入时回读 329 现只剩 328，全库搜 tag 无），已按原始导入数据补回，cp 恢复 329 台。清单位号写法坑：**用反引号 ` 代替波浪号表示区间**（`AG-002A\`E`、`TK0101\`0104`）;9 台位号写法与云端不一致但确认同一设备（走人工 alias 表给的等级），其中**新清单给出了 TBD 位号的正式号**（尾渣矿浆贮槽=TK0101~0104、水封水槽=TK0105、氢氧化镍钴储槽搅拌器带 -2- 段）——位号本身待用户拍板是否订正。
1. **账号车间白名单漏 cp bugfix**（08-10）：用户给吴广嘉（6725102258）改车间到产品，界面提示已更新但云端还是酸浸——`confirmEditUser`/`confirmAddUser` 里 v0.20 写死的 `['hpal','zh','all']` 白名单没随 v0.22 加 cp，选产品车间被静默回退。两处改为动态 `PLANTS[p] || p==='all'`；吴广嘉云端已直接修成 cp。教训：**加新车间要全局搜写死的车间 key 列表**。用户给产品车间《设备点检表》xlsx（9 sheet）→ 新建 9 个 `cp_` 前缀 v2 模板（离心泵/立式污水泵/螺杆泵/隔膜泵/搅拌器/浓密机/**压滤机/皮带机/起重机**三类首次开点检），模板 46→55。表内振动多未标限值→照惯例补 4.5/70，浓密机按表 液压油泵主电机5.0+驱动减速机·提升装置8.0；新 helper `_vibT4`（四项无异响）。频次走模板 cycleDays（abcClass 全空）：隔膜泵/浓密机 1 天、其余 3 天（≈2次/周）。**222 台开点检**：离心泵127/污水坑泵16（含液下渣浆泵927-SP）/螺杆泵11（耐驰NM絮凝剂+NaOH给料）/搅拌48/隔膜6/浓密5/压滤4/起重4/皮带1；不开：空压机3+螺旋输送/包装给料螺旋12+包装机4（**无点检表**）+静88。待用户线上验证后 push。第三车间上线——`PLANTS` 加 `cp:产品车间` + `plantPickerHtml` 改动态生成（其余 UI 全是 PLANTS 驱动零改动）。台账 xlsx（10 sheet，动设备总表+非标槽罐两张为总表）解析导入 **329 台**（`eq_cp_001~329`：动241/静88；泵154/贮槽77/搅拌48/输送13/隔膜泵6/浓密机5/压滤机·包装机·起重机·压力容器各4/非标7/空压机3；区域 207/208/209-1/209-2/212/216/217/315/542/543/927）。自动处理：总表 216/217/315 泵前后两段重复 23 位号取带采购单段、浓密机池体 5 位号不重复建、543 位号空格修复；**7 台无位号建 `927-*-90N*-TBD`**（尾渣矿浆贮槽×4/密封水槽/分配器×2）、尾渣隔膜泵位号 `PP0101~0106` 无前缀照台账保留。**全部 noInspect:true 未开点检**（等产品车间点检表）、abcClass 全空（台账无分级）。用户拍板免核对直接导入。（08-08）：`spareParts[].name` 是登记时的快照，二级库改名后老记录显旧名、统计还按新旧两个名字分开算 → 新增 `mlSpName(p)`（有 `ssId` 就实时取 `secondStockCache` 现名，手填/条目已删回退快照），详情/编辑清单/备件消耗汇总+导出/零件时间线/寿命图/单向阀厂家对比 7 处全换，`mlSpareOwner` 也按现名判归属；`openMlEdit` 顺手把缓冲区名字刷成现名（编辑保存一次云端快照自愈）。无迁移、无 rules 变更。
1. **中和车间点检全面上线 v0.21**（08-08）：用户给中和《点检表》xlsx（17 sheet）拍板"全部按他们的表来"→ 新建 14 个 `zh_` 前缀 v2 模板（浓密机·电驱 8.0 限值含提升装置+扭矩传感器；浸出/预中和/除铁铝搅拌带润滑油泵+机封水封，**推翻 08-07 敞口口径**、首批 20 台已换模板；离心泵/螺杆泵/污水坑泵/污水坑搅拌/水封/敞口搅拌/稀释装置+熟化槽(备用无设备)/空压机×3/凉水塔风机），模板 32→46（rev 恒 2，云端直接写无 marker）。**210 台开点检**：泵141(絮凝剂泵→螺杆23/污水泵→污水坑14/其余离心104)、搅拌器44、浓密机10(全A类)、空压机10、冷却塔5；95 台不点检(静82/起重机2/成套9/空压机辅机10/过滤器2)。线上老代码直接可用（模板云端解析），**待用户线上验证后 push**。
1. **多车间权限重构 v0.20**（08-07 晚，00f4e95）：账号带 `plant` 归属 + 新角色 设备工程师/设备技术员（权限相同）+ 经理升级跨车间管理（动不了 admin 账号）+ 硬删收紧仅郝行龙 + 出自家车间只读参观 + 登录自动落自家车间;业务闸口 isAdmin 全面换 `canManage` 系 helper,账号弹层动态角色/车间选择器。云端 9 账号同步:李宁翔/鹏总→经理·all、陆壮→酸浸技术员、迟广增→中和工程师、魏书国(=魏书铭)→中和技术员、4 废弃"维修员"→酸浸检修员工。详见角色矩阵段。
1. **多车间支持 v0.19：中和车间接入**（08-07）：单库 + `plant` 字段区分车间（老数据缺省=hpal 零迁移），9 订阅过滤 + 15 写入路径 `stampPlant` 盖章;顶栏徽章 + 「我的」切换车间弹层（选完 reload）;`noInspect`/`inspectTplKey` 设备字段控制点检范围;三个体验修复——预览横幅改左下角胶囊、账号快照(`ems_user_v01`)秒进消登录页闪现、**订阅挪到种子检查前**消 2 秒数据空白（日常打开也快了）。中和台账 315 台（动233+静82）解析后**已导入 305 台**（`eq_zh_001~305`，池体 10 行不重复建、2 处撞号加 TBD）;**首批点检 20 台** = 预中和/一段/二段除铁铝槽搅拌器（`inspectTplKey:'agitator_open'`，用户确认敞口无机封），浓密机（中心机电驱动 5 点，与褐铁矿液压模板不符）与其余设备 `noInspect:true` 暂不点检。**同日跟进**（af55c1e）：顶栏加「👈 点此切换车间」提示（窄屏隐藏顶栏日期让位）;中和侧看板显示占位「待添加重点设备」、检修页高压釜重点设备条只在 hpal 显示。
1. **第2周润滑计划（7.27-8.1）补录 102 笔**（08-07，纯云端数据·无代码变更）：6 页扫描件识别 → 核对表用户确认 → preview 会话 writeBatch 录入；口径复刻第1周批（lubeAt=实际日 10:00、何甫林按名字记、油品 Mobil Lux EP 2、备注「第2周润滑计划（7.27-8.1）补录」）。已执行 102（7-27:46/7-28:15/7-29:41，含手写补充 AG-204B、AG-203 机架 2 笔），未执行 24 行不录。**位号笔误 213→216**（同上周 313→316）；warman 泵两行都印「前轴承」按前/后录；絮凝剂 003F/003J 机架实打 16g 用户确认属实。lubehistory 514→**616**。新遗留：205A 电机后缺加油孔、絮凝剂 003G 机架缺油嘴（见待办）。同日：缺油嘴/加油孔 4 点录入「当个事儿办」；**絮凝剂二次稀释泵 202-PE-PP-103A 现场核实确实无加油孔（同型号仅此一台）→ 电机前/后 2 点标免维护**（原值 24g/125天留档），免维护点 94→**96**，第1周遗留③闭环。
1. **检修备件出库笔按「检修日期」记账**（08-06 晚）：补录的检修记录（8-4 干活、8-6 登记）原来账本投影笔落 `Date.now()`=登记日 → 改为跟 `ml.repairDate` 走（`applyMlStockPlanToBatch` 内 `ssDateStrToTs(ml.repairDate, 旧笔时刻 ?? now)`，只挪日期保留时分秒；日期为空/未来则回退原行为），≠今天时写 `recordedAt` 自动显「补登」标签。因投影是整体重写，**以后改检修日期账本跟着改**。**存量老笔不迁移**——打开该检修记录点「编辑→保存」一次即刷新（数量没变则结存零变动）。无字段/rules 变更。
1. **二级库出入库可补登日期 / 改已登记记录的时间**（08-06）：**仅工号 6725102247（郝行龙本人，不给其他 admin）**——`SS_TIME_EDITOR_EMPNO` + `canEditSSTime` 按工号硬判；领用/入库表单多一个日期栏（默认今天、不能选未来），详情页每笔记录多一个「改时间」按钮 → `showSheet('ssEditTime')` 弹层，**只挪日期保留原时刻，不动数量/结存/操作人**；带 `mlId` 的检修投影笔禁改（去检修记录改）。新增可选字段 `recordedAt`（真实录入时间），有它的记录显「补登」标签 + 「MM-DD 补登」小字。顺手补齐 `.hist-tag`/`.hist-action-btn` 的 CSS（原本没定义，撤销/删除是浏览器默认丑按钮）。无 rules/迁移变更。
1. **数据看板独立为顶级模块「重点设备看板」**（08-03）：新 `data-module="board"`，原 `#ml-board-view` 整块搬入（`renderMlBoard` 一行未改），导航**第一位**加「重点设备看板」（窄屏该项 `flex:0 0 86px`）；检修页删掉「📊看板」按钮回退为两视图、`applyMlView` 拆掉 board 三态；`switchModule` 加 board 渲染分支 + `subscribeMaintenance` 回调单独接实时刷新线；`mlbBackToBoard` 直跳 board 模块，从看板进单台档案时导航高亮停在看板；默认落地页仍是「设备」。同批顺带 push 上次遗留：当个事儿办列表按设备 A/B/C 等级排序（`tdClassRank`）。
1. **「当个事儿办」支持关联多台设备 + 卡片显设备名**（07-30）：`todos` 新增 `equipments[]`（老记录 `tdEqs()` 兼容读、保存镜像首台到旧字段，零迁移零 rules 变更）；设备选择弹层待办侧多选（点选/取消+完成计数）、检修侧仍单选（顺手修 `eqPickTarget` 不重置的路由 bug，入口改 `mlOpenEqPicker`）；表单设备胶囊 × 移除；卡片/详情多台位号同前缀省略（`tdAbbrevStrs`）+ 各带 ABC 徽章 + 「共 N 台」，卡片新增设备名行（多台同前缀合并/同名显一次）。多台事项整条办结不能按台销项。
2. **二级库领用/入库后详情页结存不实时刷新 bugfix**（07-29）：`submitSecondStockTxn` 保存后只切回详情页没重渲染，且订阅回调只在详情页 active 时才重画（保存瞬间 active 是表单页）→ 切回后补调 `renderSecondStockDetail`（取 cache 最新值，兜底手算结存）。纯代码 2 行。
2. **「当个事儿办」缓办事项模块 + 账号实名**（07-27）：检修页改三视图——标题旁 📋记录/📊看板/🗒️当个事儿办 三个独立按钮（`setMlView` 直选高亮）；新集合 `todos`（可选关联设备，待办→已做好准备→已办，「待办」tab 含 ready；「不办了」=软删可恢复；仅 admin 可写，**rules 已发布**）；已办/不办了确认走应用内弹层（原生 prompt/confirm 在内置浏览器面板被拦的教训）；设备选择 sheet 按 `eqPickTarget` 路由复用。同日云端账号实名：总管理员→郝行龙、翔总→李宁翔（VIP 欢迎卡文案未动）；随后按用户指令**历史快照同步刷新**——检修记录 createdByName 10 条 + 二级库历史 operatorName 16 条 全部 总管理员→郝行龙（云端 0 残留）；**唐仕彬（6725051412）admin→检修班长**；现 admin 两人=郝行龙+陆壮（6725111947，用户确认保留；检修/二级库登记只有 admin 能做）。
2. **第1周润滑计划序79~140 补录 56 笔 + 18 点标免维护**（07-27，纯云端数据·无代码变更·用户已验收）：OCR 版 xlsx 62 行扣 6 行原表作废的"免维护电机"行 = 56 条，**口径完全复刻 7-22 那批**（`lubeAt` 回填实际加油日、何甫林 `operatorId:''` 按名字记/张承钢用工号、油品记「美孚锂基脂 Mobil Lux EP 2」、备注「第1周润滑计划（7.20-7.25）补录」）；其中 **8 条与张承钢 7-23 一键登记重复 → 按用户拍板删旧笔改写为何甫林实测量**。**位号笔误订正：序99/100「冲洗水泵 313」实为 `203-PE-PP-316`**（313A/B 是"高压釜搅拌冲洗水泵"只有泵油点，316 才有电机脂点，用户当场指出）。另按用户指令：**302 电机前/后轴承张承钢那 2 条删除**（与纸质表"免维护打斜杠"冲突）+ **闪蒸密封槽搅拌器 3 台（AG-102/202/302）与浓硫酸输送泵 6 台（PP-110A/B、210A/B、310A/B）的 18 个电机轴承点标 `maintenanceFree`**（对齐现行约定：status'免维护'+标准量/周期置空+油号保留；标前均 0 历史，原值 12.5g、20g/125天留档）。lubehistory 468→**514**，lubepoints 恒 891，免维护点 76→**94**。
3. **检修模块数据看板 + 备件归属规则**（07-23）：Repairs 标题旁「📊 看板」同页切换可视化看板——1/2/3系列筛选（无「全部」）、概览卡、超寿命预警、零件占比环形图、各台条形图（点行进单台档案）、单向阀分厂家/规格寿命对比（三系列合计）、近6月趋势，手写 SVG 零依赖、纯前端无云端变更；新 `mlSpareOwner` 备件归属规则（组内先命中先得），隔热组件 kw 扩充构成件（导杆/导套/衬套/活塞/分离盘/O形圈等）、单向阀加「阀盖」，合修记录不再错标备件品牌；看板机封显示名「高压釜搅拌器机械密封」。
4. **润滑分组卡副标题口径 bugfix + 202 低预给料槽搅拌机架润滑点整改**（07-23，现场反馈+用户拍板）：①副标题原取排序后首点位状态、与「最严重点位」徽章矛盾（未初始却显"全部正常"），改与徽章同口径。②现场核实该机架**只有一个加油口**：3 台的机架上轴承 `lp_0283/0285/0287` **彻底删除**（连带 A/B 两条 2025-12-19 种子占位假历史一并删），机架下轴承 `lp_v13_001~003` **改名「机架轴承」+ 标准量 230→570g**（何甫林 7-21 三笔真实历史点名快照同步刷），每台=机架轴承+电机前/后 3 个脂点。lubepoints 894→**891**。也解释了第1周计划表该点 342g 计划/355g 实际的量（一个口打全部）。
5. **徐超工具集装箱二级库 55 条整体转移 + 垫片盘盈 +1**（07-22，纯云端数据·无代码/SEED 变更）：代码有下划线的 12 条垫片/密封环 → 203-1，其余 43 条 → 202集装箱；931.321.342 垫片盘盈结存 1→2 并补一笔入库。核验后 202集装箱 44→87、203-1 101→113，原库位清空。
6. **第1周润滑计划 71 点补录 + 海湾原矿浆泵电机点补建**（07-22，实际润滑 7-20/21，何甫林执行·非注册按名字记）：扫描版计划表 78 行登记 71 笔；**计划表 001F/G/H/L = 海湾 201B-PE-PP-002A~D**（现场对 9 台原矿浆泵连续编号），补建海湾电机前/后轴承 `lp_v21_001~008`，lubepoints → **894**；油嘴丢失 2 处/疑似漏扫 4 行/103A"无加油孔"/下轴承 230g vs 342g 等遗留见待办。
7. **界面「巡检」全站改名「点检」**（07-21，领导要求，已上线）：15 处显示文字；函数名/`inspect*` 集合名/字段名/注释未动，纯显示改动。
8. **201 给料槽搅拌 B/C 机架轴承登记**（07-16）：晨曦-B/C 上轴承≈4950g/下≈2970g，SHC 220，下次 12-23；至此 6 台给料槽搅拌机架轴承全打完。**「一管」口径修订为 ≈550cc≈495g**（旧账不追改）。
9. **218 回用/罗茨/反洗 9 台 27 点首次润滑补录**（07-16，实际 7-15，张承钢）：轴承室 9 + 电机前/后 18；SEED 旧状态顺手修正；反洗泵轴承室加注量未记录=标准量待定。
10. **褐铁矿浓密机 3 台补建主电机润滑点**（07-16）：`lp_v20_001~006` 前/后轴承 51g/90天 + 当日实际打脂登记。lubepoints → **886**。
11. **一批润滑点定标/结构修正**（07-16）：PAC 减速机 300g、PAM 200g（2#锂基脂/60天）；絮凝剂搅拌 12 台机架轴承 30g/80天；罗茨风机轴承室 VG220 20L；回用水泵 3#脂 50g/30天；11 个免维护潜水电机移出待补；**PAC/PAM 机架轴承 5 点现场核实无此结构已删**；液下渣浆泵拆上/下轴承（`lp_v19`）。待补油号 54→10。
12. **高压釜给料泵 6 台补建 ABB 电机润滑点**（07-16）：`lp_v18_001~012`，驱动端 90g/非驱动端 75g/80天；**非驱动端是绝缘轴承 6322/C3 VL0241，换轴承必须买绝缘型号**；出厂脂 UNIREX N2，等效使用油定为**壳牌 Gadus S5 V100 2（需采购）**。同日纯云端对账修正 43 条润滑记录 + 高预 18 台初次加油 A/B 归位。
13. **15 台 warman 轴承箱换油号**（07-14）：中预 6 + 高预 9 统一 Shell Omala 150（使用油号 Mobil Gear EP150）；**高预从 46# 液压油跨度换 150# 齿轮油，现场换油要放净**。同日纠错高预 warman/五二五 规格整组对调：warman 改回 1.7L/80天、五二五改回 20L/330天（NUTO H46）。
14. **设备名直观化改名三批完成 342/422 台**（07-09~10）：201 区 20 台（晨曦/海湾前缀）、202 区 44 台、203 区 278 台（N系列前缀；预热器给料泵改分级泵写法）；lubehistory/inspectmonthly 等历史快照一并刷新；`ML_KEY_EQ_GROUPS` 两处改包含式；VE-103A/CM-201 两台用户留不改。
15. **删 SEED_NEW_EQUIPMENTS_V2 灾备隐患**（07-09）：无 marker 判重、极端场景会重灌 2026-04 旧占位数据，整块删除。
16. **五定表正式交表流程定型**（07-09）：**不做替代表，直接往设备室模板副本灌系统数据**——跑 repo 根 `填五定表_模板填充脚本.py`（gitignore）；周期列填纯数字=天、日期写真 datetime、模板公式/保护全不动。**以后每次交表重跑此流程**（细节/openpyxl 坑见 HISTORY.md）。
17. **润滑页「导出五定表」按钮**（07-09）：`exportLubeWudingXlsx` 复刻模板结构 0 公式（内部自助/备用格式）；SheetJS CDN 换 **xlsx-js-style@1.2.0**（loadXlsxLib 先加载 cpexcel.js 兜底旧 .xls 中文）；Fuchs Renolit 21 点补「（锂基脂）」后缀归位脂 tab。
18. **检修联动账本"重写投影"式 + 闭环收死**（07-08）：每次保存/作废/恢复检修记录 → 删该 mlId 全部旧账本笔 → 按当前 spareParts 重开出库笔（每备件恒一笔=当前用量，删记录笔消失+库存回补）；检修记录无彻底删除；带 mlId 账本笔禁手动撤销/删除（唯一修改入口=改检修记录本身）。
19. **检修选二级库备件自动扣库存 + 检修权限收紧仅 admin**（07-08）：台账差额法 + writeBatch 原子写 + 结存不足拦截；手填备件不扣库存。
20. **二级库出入库历史页 + 润滑历史页**（07-08）：`parts-history` / `lube-history` module，全员可见，类型 tab + 自然日筛选 + 按天分组 + 导出；详情页 from 参数原路返回。
21. **点检到期改自然日 + 早 6 点**（07-07）：`calcNextInspectAt` = 上次点检日历日 + N 天的 06:00；`formatRelTime` 同步按自然日算。
22. **静设备移出点检 + 趋势页勾选测点 + 等级筛选**（07-06）：点检范围 318 台（A30/B69/C219）；趋势页 chk 测点显每日点条；列表加 A/B/C 筛选；历史/异常卡可点进趋势；检修重点设备条/零件时间线同日上线。
23. **点检模板按现场实况精修**（07-05）：离心泵 5 密封变体/搅拌器 4 变体等；34 台不做点检；`INSPECT_TPL_REV` 修订号机制；**迁移函数加 localhost 一律不跑守卫**（生产模板误升级事故当晚已回滚闭环）。
24. **点检升级数值点检 v2**（07-04）：v2 模板 + `inspectmonthly` 月表 + 频率按等级 A=1/B=2/C=7 天 + 超限标红强制备注 + 趋势页 + 导出；已上线真实使用。顺手补 `inspectExecBuffer` / `lubeExecBuffer` 的 window 桥接。
25. **21 台高压釜搅拌减速机油号更正 PAO**（07-03）：ISO VG 320 PAO / Mobil SHC Gear 320；**PAO 与矿物油不混用，换油放净**；加注量 150/130L 之谜 08-17 已解（两种 Flender 机型，见待办段末的销项条）。
26. **修 6 条阀门油压站电机轴承 oil→grease**（07-02）；**补回删工单时误删的 `formatFullTime` / `formatShortTime`**（教训：大块删模块前对通用小工具函数先 grep 全部调用方）。
27. **检修列表 A/B/C 筛选 + 备件消耗汇总导出**（07-01）：`mlClassFilter` 可与类别叠加；`openMlPartsSummary` 按 name+unit 合计导出给采购。
28. **删工单模块 → 建检修记录模块 + 设备 ABC 分级**（07-01）：工单 3 section + ~20 函数 + 种子整删，云端集合已清空；检修模块复用骨架（现行设计见上方导航注记）；`abcClass` 352 台写云端。
29. **更早改动**（2026-04 ~ 06：润滑分组/免维护/电机拆点/加注量审核/二级库建立/删工器具旧备件/油号统一/铭牌补齐 104 台/安全规则上线/巡检 v11/角色重构/企业微信集成等 48 条）：**完整纪要全部在 `HISTORY.md`**。

**🔧 直接操作云端的技巧**：
- **首选：预览会话直接写云端** —— admin 登录 `?preview=1`（localhost）后浏览器已带匿名 auth token，可在该会话里 `import` firebase 模块直接 getDocs/updateDoc/writeBatch 批量读写云端（近期大批数据修正都走这条路）。改完硬刷清缓存再验证。注意：预览只覆盖 SEED_EQUIPMENTS 显示，**润滑点等读云端**。
- 备选：给用户 F12 脚本在线上 EMS 控制台跑（脚本要 idempotent + 防呆确认 + 分批 400）。
- ~~Python urllib + API key~~ 不可用（规则要求 auth token）。
- 数据流程：改云端 → 回读核验 → 同步本地 SEED → commit + push。

**已知待办**：
- ✅ **Firebase 读取量：优化已上线并验证通过（08-11 晚）**。**额度事实**：Spark 上限 **读 5 万/天 · 写 2 万/天（读写两个独立的桶）**；**重置在美国太平洋时间零点 = 印尼中苏拉威西下午 3 点**（冬令时下午 4 点）——**不是早上 8 点，此前本文记错过**；控制台「过去 24 小时」是滚动窗口≠当日已用。**计费口径**：按**服务器返回的文档份数**算，跟"一次查询"还是"一条条 getDoc"无关；空结果最低也算 1 次读；订阅挂着期间只按变化的份数算（**所以页面开着几乎不花钱，关掉再开最贵**）；`persistentLocalCache` 命中缓存 0 读，**但监听断开超过 30 分钟再重连按全新查询计费**（车间一天开几次、间隔都超 30 分钟 → 缓存救不了额度，只能靠少订文档）。⚠️ `getCountFromServer` 聚合查询有**独立限流**，它报 `Quota exceeded` 不代表全局停服（曾据此误判过一次）。**已做**：种子判空 limit(1) + 按需订阅 + lubehistory 限 90 天 + 缓存 API 升级 + **按车间订阅 equipments/lubepoints（含 plant 补写迁移）**（见上方两条 v0.24 条目），冷启动 9600 → **435/318/340**，预期日读取从 21 万压到 5 千以内。**✅ 已验证（08-11 晚）**：marker `plant_backfill_v24_20260811` 的 `stats` 字段留了执行快照，直接读该文档即可复核（**比看控制台可靠——迁移只跑一次，之后启动静默跳过，`[Plant]` 日志再也不会出现**）：`equipments 补写 422 → {hpal:422, zh:305, cp:321}` / `lubepoints 补写 891 → {hpal:891, zh:486, cp:441}`。判据是**补写数恰等于酸浸台数、且 zh/cp 一条没被误改**，两条都成立。（`cp:321` 而非 327 是因为迁移执行时产品车间的人正在删设备，与迁移正确性无关；现在产品只剩 66 台。）线上实测酸浸设备页显示 422 台、区域分布合计吻合。**剩余观察项**：过一两天看控制台读取量确认稳定降下来。**Blaze 若要绑必须同时设预算告警 $10/月 + Firestore 每日读取硬上限**。**自建服务器/自备电脑做数据库已评估并否掉**（$3/月 vs ¥80/月 + 重写数据层 + 破坏单文件部署工作流 + 需 24h 运维）。给车间的应急话术：**"点检期间别刷新别关页面"**（读爆了已打开的客户端仍能提交，只有新开/刷新的进不去）。
- ✅ ~~设备详情页「润滑点」tab 是死代码~~ —— **08-12 已修好上线**（见「最近做的改动」第 1 条）。
- 📝 **中和/产品润滑导入遗留（08-10）**：①**中和冷却塔 `109-545-JP-TO-002B` 脂表有 2 行「电机」内容完全相同，而 `TO-002A`/`TO-002C` 一行都没有** —— 多半有一行是那两台之一的笔误，现按「电机1#/电机2#」两个点建在 002B 上，**问中和确认后挪**;②**产品 4 组位号写法差异**已走 alias 映射（隔膜泵 `109-927-PE-PP-0101~0106`↔云端 `PP0101~0106`、液下渣浆泵 `109-927-PE-SP-0101/0102`↔`927-SP0101/0102`、起重机 `109-927-PE-CN-0101`↔`927-CN0101`、氢氧化镍钴储槽搅拌器带 `-2-` 段↔云端缺）——**润滑表给出的正是正式写法，与下方产品车间遗留②③是同一件事**，台账位号一旦订正，`lp_cp_*` 的 tag 要同步改;③**53 条记录未导入**（产品 26 + 中和 27：填「出厂已加油」或只填动作没填日期，无 lubeAt 无法记），对应点位停在「未初始」;④中和 `109-206-PE-PP-005A/B`、`007A/B/C` 等 5 个点第1次润滑栏只填了「补油」没填日期，同上未导。
- 📝 **制酸车间遗留（08-12 接入 52 台）**：①**只导了 A/B 类，C 类 73 台没导**（用户拍板）——原始清单 `制酸车间动设备清单.xlsx` 在**企业微信** `Documents\WXWork\1688857201880602\Cache\File\2026-08\`（不是个人微信），要补随时可导。②**C 类那批的位号问题没解决**（真要导时再处理）：`606-L-01~03` 在 604/606 两个子项撞号、`X-608101/201/301` 一个装置位号下挂 3 行不同设备（计量泵×2+搅拌器）、`P-010002ABC` 疑似笔误（其他都是 6xx 子项号）。③**点检表还缺 3 种**：废水输送泵 3 台、应急事故水泵 3 台、含酸污水泵 1 台，现按用户指示先挂 `zs_pump_basic`，正式表到位后换。④**有 2 张点检表在动设备清单里找不到对应设备**：「掺冷风机」「炉前风机」，清单可能不全。⑤**润滑五定表没给**，润滑模块暂空。⑥🔴 **制酸账号一个都没有**（08-12 核实，四个车间里唯一空缺）——设备和模板都上线了但没人能登录，要向车间要人员名单（姓名+工号+岗位）后建号。**08-17 观察到 `users` 已涨到 48 个（08-12 是 43），有人建了 5 个账号但未核是不是制酸的 —— 下次顺手查一下 `where plant=='zs'`，是的话本条可销。**⑦位号待车间确认：610 子项无横杠（`P61001`/`T61001`）是否要补成 `P-61001`；`P-609002-TBD`（含酸污水泵）的正式位号。
- ⚠️ **产品车间台账只剩 66 台的后续（08-11 晚）**：①**271 个孤儿润滑点**（`lubepoints` plant=cp 共 441，其中 271 个的 `tag` 在台账里已不存在）——用户明确说**先保留不动、也不用列清单给车间**；润滑功能不受影响，但设备详情/检修关联/ABC 统计会断。②**车间要用起来大概率会回头要台账**（登记检修、领备件、查铭牌都需要设备记录）——恢复源 `产品车间设备台账（新）.xlsx`（微信 2026-08 文件夹），我可按 `eq_cp_NNN` 原编号重建。③删掉的点检记录备份在桌面 `产品车间台账收窄备份_2026-08-11.json`，**车间自删的 239 台不在备份里**（只能从 xlsx 重建）。④**以后动产品车间数据前先核对总数**，车间的人会自己在生产库删东西。
- 📝 **产品车间遗留**（08-11 点检**收窄为 66 台**=车间自报的 A11+B55、ABC 分级、润滑 441 点均已上线）：①**待产品车间补点检表**——(a) 已用「（临时）」模板先开的 **空压机 3 台**（`cp_compressor`，照中和无油螺杆改）和 **压滤机破碎双螺旋输送机 8 台**（`cp_screw_conveyor`，照皮带机改），**正式表到位后替换模板内容**；(b) 仍未开的 **包装给料螺旋 4 + 包装机 4**（不在车间那张 A/B 类表里，也无点检表）;②**位号订正待拍板**：新分级清单给出了正式位号——尾渣矿浆贮槽 `TK0101~0104`（云端现 `927-TK-901A~D-TBD`）、水封水槽 `TK0105`（云端 `927-TK-902-TBD` 密封水槽）、氢氧化镍钴储槽搅拌器 `109-209-2-SF-AG-001A/B`（云端缺 -2- 段）、除铁铝后液贮槽 `109-216-PE-TK-001C/D`（云端 `109B-216B-PE-TK-002A/B`，两种写法都合法未必要改）;③**7 台无位号**（尾渣矿浆贮槽×4=927-TK-901A~D-TBD、密封水槽=927-TK-902-TBD、1#/2#分配器=927-OT-901/902-TBD，建议位号是 AI 编的）+ 尾渣隔膜泵 `PP0101~0106` 无区域前缀（**08-11 的 A/B 类明细表第三次给出正式写法 `109-927-PE-PP-0101~0106`**，与润滑台账、分级清单三方一致，可以订正了——改 tag 时记得同步 `lubepoints`/`lubehistory` 的 tag）+ 氢氧化镍钴储槽搅拌器 `109-209-SF-AG-001A/B` 缺 -2- 段，正式位号待产品车间定。（~~④产品车间账号一个没建~~ —— **08-12 核实已有 6 个**：祝凯/吕聪/吴广嘉/唐勇/张甲宏/吕明，08-11 点检 130 条就是他们做的。）
- 📝 **中和车间遗留**（08-08 点检已全面上线 210 台 + 08-10 润滑 486 点，浓密机模板/扩面两项闭环）：①**TBD 位号 3 台**待中和给正式位号——污水泵 `109-204-PE-PP-015A/B-TBD`、循环水加药装置 `109-545-JP-FR-001-TBD`（台账内撞号，郝行龙拍板一台保留一台 TBD；**注意润滑表把 4 台 PAC/PAM 加药计量泵挂在 `FR-001` 上，实际归属是这台 TBD，导入时已改挂**）;②~~中和工人账号待建~~ **08-12 核实已有 5 个**（迟广增/李帆=工程师、魏书国=技术员、姚小喜=检修班长、代龙权=检修员工）;③首批 20 台搅拌器 08-07 前用 agitator_open 记的 1-2 天月表数据与新模板 `zh_agitator_leach` 测点 key 不匹配（趋势页看不到，量极小不迁移）;④**点检页类型筛选出现「泵 125 / 离心泵 16」两栏是正常的**——中和自己把 16 台底流泵的 type 改成了「离心泵」（08-10），用户确认不用管，不影响点检（模板靠 inspectTplKey 挂载）。⑤**跨区同名的笼统设备名待问中和**——污水坑搅拌器 ×10（204 区 `AG-002A~F` 6 台 + 206 区 `AG-001A~D` 4 台）、污水泵 ×10（`PP-015A~F` + `PP-019A~D`）、絮凝剂贮槽 ×5，台账原名就笼统，A~F 是不是 1#~6# 号污水坑**无书面依据不自行编号**（08-11 拆范围名时刻意跳过）；另**底流泵/溢流泵 A/B/C 三台同名是正常的**（同岗位并联/备用泵，酸浸同类也这样，**不要拆**）。
- 📝 **第2周润滑计划遗留（08-07，问何甫林）**：①高预三级泵（五二五）`203-PE-PP-205A` 电机后轴承**缺加油孔**（序45 未打，同泵前轴承正常打了）；②絮凝剂搅拌 `218-PE-AG-003G` 机架轴承**缺油嘴**（序109 未打）——**①② + 第1周晨曦C/海湾C 油嘴丢失 2 点，共 4 条已录入「当个事儿办」**（08-07，一点一条，修好即可逐条办结）；③warman 5 台轴承箱 + 五二五 201B/202B 电机前轴承 + 高压釜搅拌 201G 电机后轴承 + 高压釜给料泵电机 206A/B、306A/B 共 16 点「机子在运行/已跟生产沟通暂时无法加油」——**停机窗口补打后要补登**；④絮凝剂 003F/003J 机架轴承计划 30g 实打仅 16g（用户确认记录属实），量偏少是否补打待定
- 📝 **第1周润滑计划遗留（07-22，问何甫林）**：①晨曦C电机后轴承（lp_v16_006）+ 海湾C电机前轴承（lp_v21_005）**油嘴丢失**待补油嘴后补打补登；②原记 5 点计划表缺行，**07-27 序79~140 已补录其中 4 点**（絮凝剂 203A电机后、203B电机前后、304A电机前后），**仅剩 304B 电机前**未见（304B 位号系统内存在）。~~③絮凝剂103A 无加油孔~~——08-07 用户现场核实**确实无加油孔（同型号仅此一台）**，电机前/后 2 点已标免维护，闭环
- 📝 数值点检暂缓设备视推行情况补做：潜水泵 9（要设计水下泵简化表）、成套设备 4、给料器 1
- 📝 218 区 54 台 TBD 临时位号待用户分配正式位号（含 spec 字段缺失 14 条）
- ✅ ~~高压釜隔室搅拌减速机加注量 150/130L 不一致~~ —— **08-17 查明不是笔误，是两种减速机型号，用户明示"不必在意"，此条销项**：1、2 隔室（A/B）= Flender H2SV 10 B / 132kW / 铭牌约 119L / 台账 150L（6 台）；3~7 隔室（C~G）= Flender H3SV 9 B / 110kW / 铭牌约 110L / 台账 130L（15 台），三系列规律一致。台账值比铭牌高 18~26%（多半含油管或备油余量），换油以油标液位为准，**不要再提议改成铭牌值**
- 📝 10 个润滑点待补油号（218 悬挂起重机 4 + 546 未装机雨水回用泵 6）；另反洗泵 3 点轴承室**加注量待定**（standardAmount=null）
- 📝 罗茨风机说明书要求「皮带端/驱动端黄油杯 360h 补 2#脂」——现场是否确有黄油杯部位待核实，有则另建润滑点
- 📝 `getLubeStatus`「即将」阈值固定 30 天 → 周期 30 天的点刚润滑完立即显示"即将·剩29天"，要不要按周期比例改待用户定夺
- 📝 检修看板寿命统计**不区分「预防性提前更换」和「用坏更换」**——306B 单向阀"平均寿命10天"实为提前换拉低的；记录攒多后要不要按 category 剔除/区分预防性更换，待用户用一段时间定夺。另：分厂家寿命对比已泛化为 `mlbBrandStats`/`mlbBrandCardHtml(stats, gkey, pkey, title)` 通用函数（现有单向阀+预热器泵机封两张卡），隔膜/隔热组件要加就在 `renderMlBoard` 里多调一行
- 📝 浓硫酸给料泵推进液箱：现场实际用 NUTO H46 vs 台账 Nyvac FR 200D（涉及抗燃安全），待现场确认是否改标准油号
- 📝 63 台正式位号动设备尚未润滑（203 区冲洗水泵/污水搅拌/密封液给料泵/起重机为主），待师傅现场补 —— **07-27 已减少**（本批补录了 316 冲洗水泵、317/318A/318B/319/320 污水泵、303/304A/304B/305 污水坑搅拌等），确切台数待重算
- 📝 企业微信通知失效（notifyWeChat 无调用方，详见"关键常量"段）——用户决定**先留着**，恢复或删除待定
- 📝 firestore.rules 里 workorders 白名单待删（下次动 rules 时顺手）
- ✅ ~~iOS Safari 登录失败/点不动/一直连接中~~ —— **08-12 三刀全部闭环并线上验证**（见「最近做的改动」前三条）。**收尾结论：用户登录页诊断行为空 = 两个降级标记（`ems_idb_bad_v01`/`ems_net_degrade_v01`）一个都没置位**，跑在默认最优形态（持久化缓存 + WebChannel），**读取额度不受影响**。因此真正的病根是**第二刀那处改动**——`getAuth` → `initializeAuth(persistence:[browserLocalPersistence, inMemoryPersistence])`，即 **Firebase Auth 卡在 iOS Safari 的 IndexedDB 上**；之前那个 `timeout` 错误码是查账号被认证拖累导致的，不是 Firestore 通道本身的问题。长轮询降级与内存缓存降级两条路径**从未被触发**，作为兜底保留。「重置连接方式」入口无需再加。
- 📸 图片上传（设备故障照片）（未做；数据看板 07-23 已上线）

---

## 🧪 测试建议

本项目没有单元测试框架（故意为之 —— 用户是非技术人员，不需要维护测试）。

AI 助理在修改代码时的**最低质量标准**：

1. **语法正确**：本机无 node，用 Python 括号/引号/注释配平校验 + preview 实际加载验证（控制台 0 报错、`typeof window.fn` 全 `function`）
2. **函数暴露**：所有 `onclick="fn()"` 的 `fn` 都在 `window.fn = fn` 里
3. **元素齐全**：`getElementById('xxx')` 调用的 `xxx` 都在 HTML 里存在
4. **module 协同**：新 module 在 `switchModule()` 的所有分支里都处理到了
5. **字段一致**：Firestore 写入的字段名与读取的字段名一致

每次交付前必须跑一遍这 5 条。

---

## 🛡 安全规则（现状）

**已上线（2026-05-03）**：匿名认证 + 集合白名单 + 兜底 `match /{path=**} { allow read,write: if false; }`，全部集合要求 `request.auth != null`。规则文件 `firestore.rules`。**加新集合必须同步 rules 并在 Console 发布；重发 rules 必须含 `meta`**（漏了会导致启动读迁移标记被拒、init 中断）。

**尚未实施（将来可加强）**：`users` 仅 admin 可写、按角色细分写权限、硬删除审计字段（`deletedBy`/`deletedAt`）。目前任何匿名认证的客户端都能写白名单集合——客户端权限判断只是 UI 层拦截，不是服务端强制。

---

*本文件刻意保持紧凑（目标 ≤35KB）：只放"下次会话要用的活知识"；历史纪要放 `HISTORY.md`；改完记得两边都更新。*
